# URAI — База знань V2: як це влаштовано і що буде далі

---

## Коротко: навіщо взагалі V2?

Зараз чат URAI працює на **V1** — старій базі. Вона непогана, але має обмеження:
- Вектори маленькі (768 вимірів) → гірше розуміє складні юридичні питання
- Модель ембедингів стара (`text-embedding-004` від Google Vertex AI)

**V2** — це нова база, яка будується паралельно, поки чат продовжує працювати. Коли V2 буде готова — ми просто перемикаємо чат на неї. Користувачі нічого не помітять, але відповіді стануть кращими.

---

## Що таке "вектор" і навіщо він потрібен

Уяви, що кожен закон — це точка у просторі. Схожі закони стоять поруч, несхожі — далеко. Вектор — це координати цієї точки (список чисел).

Коли користувач пише питання — ми теж перетворюємо його на вектор і шукаємо найближчі закони у просторі. Це і є семантичний пошук.

**V1:** вектор = 768 чисел — як точка у 768-вимірному просторі  
**V2:** вектор = 3072 числа — у 4 рази більша точність → закони "розставлені" точніше

---

## З чого складається V2

### 1. Тексти на диску (`/root/laws_raw/`)

Перший крок — зберегти сирий текст кожного закону на диск сервера.

```
/root/laws_raw/
  rada/
    1001234.txt          ← текст закону
    1001234.meta.json    ← метадані: назва, URL, дата, категорія
  kmu/
  ccu/
  supreme/
  wiki/
  positions/
  mod/
  zir/
  scrape_status.json     ← статус скрапінгу по всіх документах
```

**8 джерел:**
| Джерело | Що це | Скрапер |
|---------|-------|---------|
| `rada` | Верховна Рада — ~15 500+ законів, кодекси, постанови | `scrape_all_v2.py` |
| `kmu` | Кабінет Міністрів — постанови, розпорядження | `scrape_all_v2.py` |
| `ccu` | Конституційний суд — рішення | `scrape_all_v2.py` |
| `supreme` | Верховний суд — огляди практики (PDF) | `scrape_all_v2.py` |
| `wiki` | legalaid.wiki — юридичні терміни та поняття | `scrape_all_v2.py` |
| `positions` | lpd.court.gov.ua — правові позиції ВС (~12 800) | `scrape_all_v2.py` |
| `mod` | mod.gov.ua — накази МОУ (PDF + OCR) | `scrape_mod_v2.py` |
| `zir` | zir.tax.gov.ua — Q&A ДПС (~5 900) | `scrape_zir_v2.py` |

`scrape_all_v2.py` охоплює 6 з 8 джерел. MOD і ZIR мають окремі скрапери з власними особливостями (Playwright для PDF, POST API для ZIR). Всі 8 джерел підтримують pause/resume через JSON state-файли.

---

### 2. Колекції Qdrant (`*_v2`)

**Qdrant** — це база даних для векторів. Думай про неї як про спеціальний індекс, який вміє шукати "схожі вектори" дуже швидко.

У нас **20 колекцій V2** — кожна для своєї групи законів:

```
Рада (13 колекцій):
  rada_finance_v2     ← фінанси, банки, бюджет
  rada_state_v2       ← державне управління
  rada_personnel_v2   ← кадрові питання
  rada_court_v2       ← суди, процес
  rada_intl_v2        ← міжнародне право
  rada_labor_v2       ← трудове право
  rada_civil_v2       ← цивільне право
  rada_criminal_v2    ← кримінальне право
  rada_admin_v2       ← адміністративне право
  rada_housing_v2     ← житло, нерухомість
  rada_land_v2        ← земельне право
  rada_industry_v2    ← промисловість, економіка
  rada_other_v2       ← інше

Інші джерела (7 колекцій):
  laws_kmu_v2         ← постанови КМУ
  laws_ccu_v2         ← рішення КСУ
  laws_supreme_v2     ← огляди практики ВС
  laws_wiki_v2        ← юридичні терміни
  laws_positions_v2   ← правові позиції ВС (~12 800 позицій)
  laws_mod_v2         ← накази МОУ
  laws_zir_v2         ← Q&A ДПС
```

Розділення по темах потрібне щоб **не шукати по всьому одразу**. Коли користувач питає про трудовий договір — шукаємо тільки в `rada_labor_v2`, а не у всіх 20 колекціях.

---

### 3. Реіндекс — перетворення тексту у вектори

Реіндекс — це процес, який:
1. Читає `.txt` файл з диску
2. Ділить його на **чанки** (шматки ~3000 символів з перекриттям 300 символів)
3. Кожен чанк відправляє у модель `gemini-embedding-001` → отримує вектор (3072 числа)
4. Зберігає чанк + вектор у Qdrant

**Чому чанки, а не весь закон цілком?**
Закони можуть бути дуже довгими (100+ сторінок). Модель не може обробити все одразу. Зате якщо розбити на абзаци-чанки — можна знайти конкретну норму, а не просто "цей закон загалом про це".

**Розміри чанків по джерелах:**
| Джерело | Розмір чанку | Перекриття |
|---------|-------------|-----------|
| Рада | 3 000 симв. | 300 |
| КМУ | 3 000 симв. | 300 |
| КСУ, ВС, МОУ | 3 000 симв. | 300 |
| Вікі, Позиції, ZIR | 2 000 симв. | 200 |

**Час реіндексу:** ~50–80 годин для всіх 8 джерел (≈40 000+ документів).

---

## Схема: як все пов'язано

```
Інтернет (zakon.rada.gov.ua, kmu.gov.ua, ...)
    ↓  [Скрапер scrape_all_v2.py]
    ↓
/root/laws_raw/{source}/{law_id}.txt + .meta.json
    ↓  [Реіндекс reindex_v2.py]
    ↓  (chunk → gemini-embedding-001 → вектор 3072)
    ↓
Qdrant: 17 колекцій *_v2
    ↓  (поки — тільки Shadow, чат не бачить)
    ↓
[МАЙБУТНЄ] /ask endpoint → шукає у _v2 колекціях
```

---

## Як зараз працює чат (V1)

Коли користувач пише питання у чаті:

1. **Ембединг питання** — питання перетворюється у вектор 768 вимірів (`text-embedding-004`)
2. **Маршрутизація** — система визначає, які колекції найімовірніше містять відповідь (через центроїди)
3. **Пошук у Qdrant** — паралельно шукає у потрібних колекціях V1, повертає топ-N чанків
4. **Буст Ради** — результати із законів Ради отримують вищий пріоритет
5. **Генерація відповіді** — чанки + питання → Gemini → текстова відповідь з посиланнями

**V1 колекції (18 штук, 768 вимірів):**
- 13 колекцій Ради (без суфікса `_v2`)
- `laws_supreme`, `laws_wiki`, `laws_ccu`, `laws_positions`, `laws_kmu`, `laws_mod`, `laws_zir` (останні два — нові, додані до V1 також)

---

## Як буде працювати чат після переходу на V2

Логіка залишається такою ж, але:

| Параметр | V1 (зараз) | V2 (майбутнє) |
|----------|-----------|--------------|
| Модель ембедингу | `text-embedding-004` (Vertex AI) | `gemini-embedding-001` (Google GenAI) |
| Розмір вектора | 768 | 3072 |
| Кількість колекцій | 18 | 20 |
| Джерел | 8 | 8 (ті самі) |
| Якість пошуку | Добра | Значно краща |
| Розуміння контексту | Базове | Глибше (нова модель) |

**Що зміниться для користувача:**
- Кращі відповіді на складні юридичні питання
- Менше "промахів" коли система знаходить не те
- Більше джерел (додано позиції Верховного суду)

**Що НЕ зміниться:**
- Інтерфейс чату — той самий
- Швидкість відповіді — приблизно та сама
- Мова — українська

---

## Поточний стан (квітень 2026)

```
✅ Скрапер       — запущено, завантажує тексти на диск
                   (зараз: rada_finance, ~15 500 документів Ради)

⏳ Реіндекс V2   — ще не запущено
                   (потрібно дочекатись завершення скрапера)

🔴 Чат на V2     — ще не переключено
                   (V1 в продакшн, V2 — shadow/тінь)
```

**Порядок дій:**
1. ✅ Скрапер завантажує всі тексти (~2–3 тижні для всіх 5 джерел)
2. ⬜ Запускаємо реіндекс V2 (~3–4 дні)
3. ⬜ Тестуємо якість пошуку на V2 колекціях
4. ⬜ Переключаємо `/ask` endpoint з V1 на V2
5. ⬜ Вимикаємо V1 колекції (або залишаємо як резерв)

---

## Управління через адмін-панель

Все керується через `/admin/v2`:

- **Скрапер** — запустити/зупинити завантаження текстів. Можна вибрати конкретне джерело або всі. Логи показують кожен закон у реальному часі.
- **Реіндекс** — запустити/зупинити конвертацію текстів у вектори. Прогрес зберігається — якщо зупинити, продовжить з того ж місця.
- **Аналітика** — статистика по кожному джерелу: скільки завантажено, скільки помилок, які закони пропущено.
- **Диск** — перегляд файлів на диску: скільки займає, пошук по ID або назві, читання тексту конкретного закону.

---

*Документ оновлено: травень 2026*
